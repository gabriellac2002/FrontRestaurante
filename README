# FrontRestaurante

## Instalação do Projeto

Siga os passos abaixo para instalar e configurar o projeto FrontRestaurante em sua máquina local.

### Pré-requisitos

Certifique-se de ter os seguintes softwares instalados em sua máquina:

- [Node.js](https://nodejs.org/) (versão 14 ou superior)
- [npm](https://www.npmjs.com/) (gerenciador de pacotes do Node.js)
- [Git](https://git-scm.com/) (opcional, para clonar o repositório)

### Passo a Passo

1. **Clone o repositório** (opcional, se estiver usando Git):

    ```bash
    git clone git@github.com:gabriellac2002/FrontRestaurante.git
    cd FrontRestaurante
    ```

2. **Instale as dependências**:

    ```bash
    npm install
    ```


3. **Inicie o servidor de desenvolvimento**:

    ```bash
    npm start
    ```

4. **Acesse o projeto**:

    Abra o navegador e acesse `http://localhost:3000` para ver o projeto em execução.

### Scripts Disponíveis

- `npm start`: Inicia o servidor de desenvolvimento.

### Estrutura do Projeto

- `src/`: Contém o código-fonte do projeto.
- `public/`: Contém os arquivos públicos.
- `node_modules/`: Contém as dependências do projeto.

